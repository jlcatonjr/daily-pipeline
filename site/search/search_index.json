{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Daily Pipeline Documentation","text":"<p>Daily Pipeline provides two coordinated execution layers:</p> <ol> <li>Core protocol (Stages 1-8) implemented in <code>daily_pipeline/protocol.py</code></li> <li>Orchestration extension (Stages 9+) operated externally, with Stage 9.5 callable helper APIs available in <code>daily_pipeline/integration/sync.py</code></li> </ol>"},{"location":"#documentation-map","title":"Documentation Map","text":"<ul> <li>Getting Started</li> <li>How It Works</li> <li>CLI Reference</li> <li>Stage Contracts</li> <li>Extended Operations</li> <li>API Reference</li> </ul>"},{"location":"#source-of-truth-notes","title":"Source-of-Truth Notes","text":"<p>For this docs application:</p> <ul> <li><code>docs_src/</code> is the authoring source for MkDocs pages and navigation.</li> <li>Repository reference docs under <code>docs/</code> remain available for direct Markdown browsing.</li> </ul> <p>Execution semantics remain authoritative in:</p> <ul> <li><code>daily_pipeline/</code> implementation behavior</li> <li><code>protocols/stages.md</code> and <code>protocols/stages.csv</code></li> <li><code>configs/</code> and <code>tests/</code></li> </ul>"},{"location":"cli-reference/","title":"CLI Reference","text":"<p><code>daily_pipeline.cli</code> supports two routing paths.</p>"},{"location":"cli-reference/#team-mode-core-protocol","title":"Team Mode (Core Protocol)","text":"<p>Core protocol execution for selected team files:</p> <pre><code>python3 -m daily_pipeline.cli \\\n  --team &lt;team-a.json&gt; \\\n  --team &lt;team-b.json&gt; \\\n  --output-root tmp/daily-pipeline\n</code></pre> <p>Common flags:</p> <ul> <li><code>--teams-file</code></li> <li><code>--config</code></li> <li><code>--trigger team-create|team-update</code></li> <li><code>--allow-degraded</code></li> <li><code>--dry-run</code></li> </ul>"},{"location":"cli-reference/#sync-mode-stage-95-helper-path","title":"Sync Mode (Stage 9.5 Helper Path)","text":"<pre><code>python3 -m daily_pipeline.cli \\\n  --sync \\\n  --root \"$VK_ROOT\" \\\n  --build-team-py \"$BUILD_TEAM_PY\" \\\n  --post-audit\n</code></pre> <p>Common sync flags:</p> <ul> <li><code>--root</code> (defaults to <code>$VK_ROOT</code> or <code>.</code>)</li> <li><code>--build-team-py</code></li> <li><code>--post-audit</code></li> <li><code>--dry-run</code></li> </ul> <p>Notes:</p> <ul> <li>Routing is separated between team-mode and sync-mode behavior.</li> <li>Parser exclusivity is enforced for <code>--team</code> and <code>--sync</code>.</li> <li><code>--teams-file</code> is a team-mode input and is not consumed in sync-mode execution.</li> </ul>"},{"location":"extended-operations/","title":"Extended Operations","text":"<p>Operational runbook for orchestration extension stages is maintained in:</p> <ul> <li><code>EXTENDED-PIPELINE-OPERATIONS.md</code></li> </ul> <p>Related pages:</p> <ul> <li>Stage Contracts</li> </ul> <p>Repository source file:</p> <ul> <li><code>EXTENDED-PIPELINE-OPERATIONS.md</code></li> </ul> <p>Key lock points carried into this docs app:</p> <ul> <li>Stage 9.5 helper path in-package is callable, but orchestration ownership remains external.</li> <li>Stage-level progression is best-effort; standalone sync CLI can return non-zero on failed targets.</li> <li>Core package runtime remains independent of <code>~/.local/bin</code> script dependencies.</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#install","title":"Install","text":"<pre><code>python3 -m pip install -e .\npython3 -m daily_pipeline.cli --help\n</code></pre>"},{"location":"getting-started/#quick-core-run-stages-1-8","title":"Quick Core Run (Stages 1-8)","text":"<pre><code>python3 -m daily_pipeline.cli \\\n  --team tests/fixtures/team_a.json \\\n  --team tests/fixtures/team_b.json \\\n  --output-root tmp/daily-pipeline\n</code></pre>"},{"location":"getting-started/#quick-stage-95-helper-run","title":"Quick Stage 9.5 Helper Run","text":"<pre><code>python3 -m daily_pipeline.cli \\\n  --sync \\\n  --root \"$VK_ROOT\" \\\n  --build-team-py \"$BUILD_TEAM_PY\" \\\n  --post-audit\n</code></pre>"},{"location":"getting-started/#dry-run-examples","title":"Dry Run Examples","text":"<p>Core mode dry run:</p> <pre><code>python3 -m daily_pipeline.cli --team tests/fixtures/team_a.json --dry-run\n</code></pre> <p>Sync mode dry run:</p> <pre><code>python3 -m daily_pipeline.cli --sync --root \"$VK_ROOT\" --build-team-py \"$BUILD_TEAM_PY\" --dry-run\n</code></pre>"},{"location":"how-it-works/","title":"How It Works","text":""},{"location":"how-it-works/#core-protocol-stages-1-8","title":"Core Protocol (Stages 1-8)","text":"<p>The core protocol performs deterministic ingestion, normalization, commonality analysis, abstract synthesis, integration-request construction, stage registry output, and reporting.</p> <p>Core entrypoint:</p> <ul> <li><code>daily_pipeline.protocol.run_protocol()</code></li> </ul>"},{"location":"how-it-works/#orchestration-extension-stages-9","title":"Orchestration Extension (Stages 9+)","text":"<p>Stages 9+ are externally orchestrated.</p> <ul> <li>Stage 9: external handoff stage</li> <li>Stage 9.5: deterministic sync helper path (callable APIs in <code>daily_pipeline/integration/sync.py</code>)</li> <li>Stages 10, 10.1, 10.5, 11: cross-repository investigation/remediation operations</li> </ul> <p>Stage 9.5 helper behavior is best-effort for orchestration progression. Standalone CLI sync can return non-zero when one or more targets fail, for operator visibility.</p>"},{"location":"how-it-works/#discovery-scope-in-stage-95-helper-path","title":"Discovery Scope in Stage 9.5 Helper Path","text":"<p><code>discover_sync_targets(root_dir)</code> checks:</p> <ol> <li><code>root_dir</code> itself</li> <li>Immediate child directories of <code>root_dir</code></li> </ol> <p>A directory is considered eligible when <code>.github/agents/_build-description.json</code> is present.</p>"},{"location":"how-it-works/#runtime-boundary","title":"Runtime Boundary","text":"<p>Core package execution does not require scripts from <code>~/.local/bin</code>. External script references are optional orchestration runbook examples.</p>"},{"location":"stage-contracts/","title":"Stage Contracts","text":"<p>Canonical stage contracts are maintained in:</p> <ul> <li><code>protocols/stages.md</code></li> <li><code>protocols/stages.csv</code></li> </ul> <p>For convenience in this docs application, see:</p> <ul> <li>Core stage contract summary on this page</li> <li>Extended-orchestration context in Extended Operations</li> </ul> <p>Repository source files:</p> <ul> <li><code>protocols/stages.md</code></li> <li><code>protocols/stages.csv</code></li> </ul> <p>Boundary lock:</p> <ul> <li>Core execution remains Stages 1-8.</li> <li>Stage 9 remains external handoff.</li> <li>Stage 9.5 helper APIs are callable in-package but execution ownership remains external orchestration.</li> </ul>"},{"location":"api-reference/","title":"API Reference \u2014 daily-pipeline","text":"<p>Public API reference for the <code>daily_pipeline</code> package.</p> <p>This reference documents the supported operator/developer-facing API surface. Underscored helpers are internal and may change without notice.</p>"},{"location":"api-reference/#execution-layers","title":"Execution Layers","text":"Layer Scope Core protocol Stages 1-8 executed by <code>daily_pipeline.protocol.run_protocol()</code> Orchestration extension Stages 9+ executed externally; Stage 9.5 has callable helpers in <code>integration/sync.py</code> <p>For stage contracts, see Stage Contracts.</p>"},{"location":"api-reference/#core-modules","title":"Core Modules","text":"Module Role <code>config</code> Load runtime configuration (<code>PipelineConfig</code>) <code>protocol</code> Execute core Stages 1-8 <code>models</code> Shared dataclass contracts used across pipeline stages <code>cli</code> CLI entrypoints for team mode and sync mode"},{"location":"api-reference/#integration-modules","title":"Integration Modules","text":"Module Role <code>integration-agentteams</code> Build and write Stage 6 integration request payloads <code>integration-sync</code> Stage 9.5 callable helpers for deterministic AgentTeams sync"},{"location":"api-reference/#typical-usage","title":"Typical Usage","text":"<p>Core run (Stages 1-8):</p> <pre><code>python3 -m daily_pipeline.cli \\\n  --team tests/fixtures/team_a.json \\\n  --team tests/fixtures/team_b.json \\\n  --output-root tmp/daily-pipeline\n</code></pre> <p>Stage 9.5 helper path (external orchestration support):</p> <pre><code>python3 -m daily_pipeline.cli \\\n  --sync \\\n  --root \"$VK_ROOT\" \\\n  --build-team-py \"$BUILD_TEAM_PY\" \\\n  --post-audit\n</code></pre> <p>The sync path is a callable/helper interface for orchestration extension behavior. It does not move Stage 9.5 into core <code>run_protocol()</code> execution.</p>"},{"location":"api-reference/cli/","title":"<code>cli</code> \u2014 daily-pipeline","text":"<p>Command-line entrypoint for core protocol runs and Stage 9.5 sync helper runs.</p> <p>Source: <code>daily_pipeline/cli.py</code></p>"},{"location":"api-reference/cli/#public-entry-point","title":"Public Entry Point","text":""},{"location":"api-reference/cli/#main","title":"<code>main()</code>","text":"<p>Parses CLI flags and routes execution to one of two modes:</p> <ol> <li>Team mode (core Stages 1-8)</li> <li>Sync mode (<code>--sync</code>) for Stage 9.5 helper execution</li> </ol>"},{"location":"api-reference/cli/#team-mode-core-protocol","title":"Team Mode (Core Protocol)","text":"<p>Required inputs:</p> <ul> <li><code>--team &lt;path&gt;</code> (repeatable), or</li> <li><code>--teams-file &lt;path&gt;</code> (one team path per line)</li> </ul> <p>Optional flags:</p> <ul> <li><code>--config</code></li> <li><code>--trigger</code> (<code>team-create</code> or <code>team-update</code>)</li> <li><code>--output-root</code></li> <li><code>--allow-degraded</code></li> <li><code>--dry-run</code></li> </ul> <p>Execution target:</p> <ul> <li>Calls <code>daily_pipeline.protocol.run_protocol()</code></li> </ul>"},{"location":"api-reference/cli/#sync-mode-stage-95-helper-path","title":"Sync Mode (Stage 9.5 Helper Path)","text":"<p>Mode selector:</p> <ul> <li><code>--sync</code></li> </ul> <p>Optional flags:</p> <ul> <li><code>--root</code> (default <code>$VK_ROOT</code> or <code>.</code>)</li> <li><code>--build-team-py</code></li> <li><code>--post-audit</code></li> <li><code>--dry-run</code></li> </ul> <p>Execution target:</p> <ul> <li>Calls <code>discover_sync_targets()</code> and <code>execute_agentteams_sync()</code> from <code>daily_pipeline.integration.sync</code></li> </ul> <p>Behavior:</p> <ul> <li>Routing is separated between team-mode execution and sync-mode execution.</li> <li>Parser-level exclusivity is enforced for <code>--team</code> and <code>--sync</code>.</li> <li><code>--teams-file</code> remains a team-mode input and is not consumed in sync-mode execution.</li> <li>Process exits non-zero if any sync target returns <code>failed</code> status.</li> </ul>"},{"location":"api-reference/cli/#internal-helpers","title":"Internal Helpers","text":"<p>Underscored functions (<code>_parser</code>, <code>_resolve_team_paths</code>, <code>_run_sync</code>) are internal implementation helpers and are not part of the stable public API contract.</p>"},{"location":"api-reference/config/","title":"<code>config</code> \u2014 daily-pipeline","text":"<p>Load and normalize pipeline configuration.</p> <p>Source: <code>daily_pipeline/config.py</code></p>"},{"location":"api-reference/config/#public-types","title":"Public Types","text":""},{"location":"api-reference/config/#pipelineconfig","title":"<code>PipelineConfig</code>","text":"<p>Dataclass used by runtime code and CLI integration.</p> <p>Fields:</p> <ul> <li><code>capability_min_teams</code> (<code>int</code>)</li> <li><code>reference_min_teams</code> (<code>int</code>)</li> <li><code>confidence_floor</code> (<code>float</code>)</li> <li><code>agentteams_repo</code> (<code>str</code>) \u2014 Stage 6 integration target path</li> <li><code>build_team_py</code> (<code>str | None</code>) \u2014 Stage 9.5 helper executable path override</li> </ul>"},{"location":"api-reference/config/#public-functions","title":"Public Functions","text":""},{"location":"api-reference/config/#load_configconfig_path","title":"<code>load_config(config_path)</code>","text":"<p>Load config JSON from an explicit path, or fallback to <code>configs/defaults.json</code> when <code>config_path</code> is <code>None</code>.</p> <p>Args:</p> <ul> <li><code>config_path</code> (<code>str | None</code>) \u2014 optional path to config JSON file.</li> </ul> <p>Returns:</p> <ul> <li><code>PipelineConfig</code></li> </ul> <p>Notes:</p> <ul> <li><code>build_team_py</code> falls back to <code>None</code> when unset.</li> <li>End-to-end Stage 9.5 resolution order in CLI path is <code>--build-team-py</code> argument, then <code>config.build_team_py</code>, then <code>$AGENTTEAMS_REPO/build_team.py</code> fallback in <code>integration/sync.py</code>.</li> </ul>"},{"location":"api-reference/config/#authority-notes","title":"Authority Notes","text":"<ul> <li>Config defaults: <code>configs/defaults.json</code></li> <li>Team input schema: <code>configs/team-schema.json</code></li> </ul>"},{"location":"api-reference/integration-agentteams/","title":"<code>integration.agentteams</code> \u2014 daily-pipeline","text":"<p>Build and persist Stage 6 integration request payloads.</p> <p>Source: <code>daily_pipeline/integration/agentteams.py</code></p>"},{"location":"api-reference/integration-agentteams/#public-functions","title":"Public Functions","text":""},{"location":"api-reference/integration-agentteams/#build_integration_requestabstract_team-trigger-target_repo-confidence_floor","title":"<code>build_integration_request(abstract_team, trigger, target_repo, confidence_floor)</code>","text":"<p>Create an <code>IntegrationRequest</code> dataclass containing a filtered payload for AgentTeams update-request workflows.</p> <p>Args:</p> <ul> <li><code>abstract_team</code> (<code>AbstractTeamOutput</code>)</li> <li><code>trigger</code> (<code>str</code>)</li> <li><code>target_repo</code> (<code>str</code>)</li> <li><code>confidence_floor</code> (<code>float</code>)</li> </ul> <p>Returns:</p> <ul> <li><code>IntegrationRequest</code></li> </ul> <p>Behavior:</p> <ul> <li>Includes shared capabilities/references above confidence floor.</li> <li>Preserves mapping and provenance metadata.</li> <li>Stores trigger metadata (<code>team-create</code> or <code>team-update</code>) in the payload while current action remains <code>agentteams-update-request</code>.</li> <li>Adds constraints:</li> <li><code>reference_only_external_scripts</code></li> <li><code>no_private_imports</code></li> </ul>"},{"location":"api-reference/integration-agentteams/#write_integration_requestrequest-out_dir","title":"<code>write_integration_request(request, out_dir)</code>","text":"<p>Write JSON payload to disk.</p> <p>Args:</p> <ul> <li><code>request</code> (<code>IntegrationRequest</code>)</li> <li><code>out_dir</code> (<code>str</code>)</li> </ul> <p>Returns:</p> <ul> <li><code>str</code> \u2014 path to written JSON file (<code>agentteams-request-&lt;trigger&gt;.json</code>)</li> </ul>"},{"location":"api-reference/integration-sync/","title":"<code>integration.sync</code> \u2014 daily-pipeline","text":"<p>Callable Stage 9.5 helper APIs for deterministic AgentTeams synchronization.</p> <p>Source: <code>daily_pipeline/integration/sync.py</code></p>"},{"location":"api-reference/integration-sync/#execution-layer","title":"Execution Layer","text":"<p>This module supports orchestration extension behavior (Stage 9.5). It is callable from Python/CLI, but Stage 9.5 remains externally orchestrated and is not part of <code>run_protocol()</code>.</p>"},{"location":"api-reference/integration-sync/#public-functions","title":"Public Functions","text":""},{"location":"api-reference/integration-sync/#discover_sync_targetsroot_dir","title":"<code>discover_sync_targets(root_dir)</code>","text":"<p>Discover candidate repositories for Stage 9.5 sync.</p> <p>Args:</p> <ul> <li><code>root_dir</code> (<code>str</code>) \u2014 workspace root (commonly <code>$VK_ROOT</code>).</li> </ul> <p>Returns:</p> <ul> <li><code>list[str]</code> \u2014 absolute repo paths containing <code>.github/agents/_build-description.json</code>.</li> </ul> <p>Notes:</p> <ul> <li>Checks <code>root_dir</code> itself and immediate child directories.</li> </ul>"},{"location":"api-reference/integration-sync/#execute_agentteams_synctarget_repo-build_team_pynone-post_auditfalse-dry_runfalse","title":"<code>execute_agentteams_sync(target_repo, build_team_py=None, post_audit=False, dry_run=False)</code>","text":"<p>Run deterministic sync for one target repository.</p> <p>Args:</p> <ul> <li><code>target_repo</code> (<code>str</code>)</li> <li><code>build_team_py</code> (<code>str | None</code>)</li> <li><code>post_audit</code> (<code>bool</code>)</li> <li><code>dry_run</code> (<code>bool</code>)</li> </ul> <p>Returns:</p> <ul> <li><code>SyncResult</code></li> </ul> <p>Path resolution order for <code>build_team_py</code>:</p> <ol> <li>Explicit function argument</li> <li>Environment fallback: <code>$AGENTTEAMS_REPO/build_team.py</code></li> </ol> <p>When called through CLI sync mode, the argument passed to this function is resolved as:</p> <ol> <li><code>--build-team-py</code></li> <li><code>config.build_team_py</code></li> <li>Function-level environment fallback above</li> </ol> <p>Skip/failure behavior:</p> <ul> <li>Returns <code>status='skipped'</code> for unresolved executable, missing build description, or dry-run.</li> <li>Returns <code>status='failed'</code> for subprocess non-zero exit or execution exceptions.</li> <li>Never raises; always returns <code>SyncResult</code>.</li> <li>Stage-level orchestration contract is best-effort/non-halting; standalone CLI sync may still exit non-zero when any target fails, for operator visibility.</li> </ul> <p>Command invocation:</p> <pre><code>python &lt;build_team.py&gt; --description &lt;target/.github/agents/_build-description.json&gt; --project &lt;target&gt; --update --merge --yes [--post-audit]\n</code></pre>"},{"location":"api-reference/integration-sync/#internal-helpers","title":"Internal Helpers","text":"<p><code>_resolve_build_team_py()</code> is internal and not part of the stable public API contract.</p>"},{"location":"api-reference/models/","title":"<code>models</code> \u2014 daily-pipeline","text":"<p>Shared dataclass contracts for ingestion, analysis, synthesis, integration, reporting, and sync operations.</p> <p>Source: <code>daily_pipeline/models.py</code></p>"},{"location":"api-reference/models/#public-dataclasses","title":"Public Dataclasses","text":""},{"location":"api-reference/models/#teamagent-contracts","title":"Team/Agent Contracts","text":"<ul> <li><code>AgentRecord</code></li> <li><code>TeamRecord</code></li> <li><code>NormalizedTeam</code></li> </ul>"},{"location":"api-reference/models/#shared-analysis-contracts","title":"Shared Analysis Contracts","text":"<ul> <li><code>SharedCapability</code></li> <li><code>SharedReference</code></li> </ul>"},{"location":"api-reference/models/#synthesisintegration-contracts","title":"Synthesis/Integration Contracts","text":"<ul> <li><code>AbstractTeamOutput</code></li> <li><code>IntegrationRequest</code></li> </ul>"},{"location":"api-reference/models/#reporting-contracts","title":"Reporting Contracts","text":"<ul> <li><code>StageResult</code></li> <li><code>RunContext</code></li> </ul>"},{"location":"api-reference/models/#stage-95-helper-contract","title":"Stage 9.5 Helper Contract","text":"<ul> <li><code>SyncResult</code></li> <li><code>status</code> values: <code>ok</code>, <code>skipped</code>, <code>failed</code></li> <li><code>skip_reason</code> values when skipped:<ul> <li><code>build_team_py_not_configured</code></li> <li><code>build_team_py_not_found</code></li> <li><code>build_description_missing</code></li> <li><code>dry_run</code></li> </ul> </li> </ul>"},{"location":"api-reference/models/#notes","title":"Notes","text":"<p>These dataclasses are shared across modules and are part of the documented API contract for this repository's public interfaces.</p>"},{"location":"api-reference/protocol/","title":"<code>protocol</code> \u2014 daily-pipeline","text":"<p>Execute the core daily-pipeline protocol.</p> <p>Source: <code>daily_pipeline/protocol.py</code></p>"},{"location":"api-reference/protocol/#public-functions","title":"Public Functions","text":""},{"location":"api-reference/protocol/#run_protocolteam_paths-config-trigger-output_root-allow_degraded","title":"<code>run_protocol(team_paths, config, trigger, output_root, allow_degraded)</code>","text":"<p>Run core stages 1-8 and return report metadata.</p> <p>Args:</p> <ul> <li><code>team_paths</code> (<code>list[str]</code>) \u2014 selected team JSON files.</li> <li><code>config</code> (<code>PipelineConfig</code>) \u2014 runtime thresholds and integration target settings.</li> <li><code>trigger</code> (<code>str</code>) \u2014 integration trigger (<code>team-create</code> or <code>team-update</code>).</li> <li><code>output_root</code> (<code>str</code>) \u2014 root path for stage outputs.</li> <li><code>allow_degraded</code> (<code>bool</code>) \u2014 permit degraded mode continuation when framework matrix indicates unavailable frameworks.</li> </ul> <p>Returns:</p> <ul> <li><code>dict[str, str]</code> with keys:</li> <li><code>run_dir</code></li> <li><code>report</code></li> </ul> <p>Behavior summary:</p> <ul> <li>Stages 1-8 are executed in sequence.</li> <li>If stage 2 detects degraded mode and <code>allow_degraded=False</code>, execution exits early with blocked stage result and consolidated report.</li> </ul>"},{"location":"api-reference/protocol/#execution-boundary","title":"Execution Boundary","text":"<p>This module is authoritative for core Stages 1-8 only.</p> <p>Stage 9+ orchestration behavior is external. Stage 9.5 helper callables live in <code>daily_pipeline/integration/sync.py</code> and are not executed by <code>run_protocol()</code>.</p> <p>See <code>protocols/stages.md</code> for full contracts.</p>"}]}