<!-- AGENTTEAMS:BEGIN content v=1 -->
# SETUP-REQUIRED.md

The following **90 placeholder(s)** could not be automatically resolved
for project **MusicMaker** and require manual attention.

---

## 1. `{STYLE_REFERENCE_PATH}`

**Found in:** `multiple`
**Context:** The placeholder {STYLE_REFERENCE_PATH} could not be auto-resolved.

**Action required:** Search for `{MANUAL:STYLE_REFERENCE_PATH}` across all generated
agent files and replace with the correct value.

---

## 2. `{CONVERSION_PIPELINE}`

**Found in:** `multiple`
**Context:** The placeholder {CONVERSION_PIPELINE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:CONVERSION_PIPELINE}` across all generated
agent files and replace with the correct value.

---

## 3. `{PIP_PACKAGE_NAME}`

**Found in:** `multiple`
**Context:** The placeholder {PIP_PACKAGE_NAME} could not be auto-resolved.

**Action required:** Search for `{MANUAL:PIP_PACKAGE_NAME}` across all generated
agent files and replace with the correct value.

---

## 4. `{DOC_SITE_CONFIG_FILE}`

**Found in:** `multiple`
**Context:** The placeholder {DOC_SITE_CONFIG_FILE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:DOC_SITE_CONFIG_FILE}` across all generated
agent files and replace with the correct value.

---

## 5. `{TOOL_DOCS_URL}`

**Found in:** `tool-eslint.agent.md`
**Context:** The specialist agent for 'ESLint' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 6. `{TOOL_API_SURFACE}`

**Found in:** `tool-eslint.agent.md`
**Context:** The specialist agent for 'ESLint' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 7. `{TOOL_COMMON_PATTERNS}`

**Found in:** `tool-eslint.agent.md`
**Context:** The specialist agent for 'ESLint' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 8. `{TOOL_DOCS_URL}`

**Found in:** `tool-nodejs.agent.md`
**Context:** The specialist agent for 'Node.js' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 9. `{TOOL_API_SURFACE}`

**Found in:** `tool-nodejs.agent.md`
**Context:** The specialist agent for 'Node.js' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 10. `{TOOL_COMMON_PATTERNS}`

**Found in:** `tool-nodejs.agent.md`
**Context:** The specialist agent for 'Node.js' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 11. `{TOOL_DOCS_URL}`

**Found in:** `tool-tonejs.agent.md`
**Context:** The specialist agent for 'Tone.js' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 12. `{TOOL_API_SURFACE}`

**Found in:** `tool-tonejs.agent.md`
**Context:** The specialist agent for 'Tone.js' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 13. `{TOOL_COMMON_PATTERNS}`

**Found in:** `tool-tonejs.agent.md`
**Context:** The specialist agent for 'Tone.js' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 14. `{TOOL_DOCS_URL}`

**Found in:** `tool-vexflow.agent.md`
**Context:** The specialist agent for 'VexFlow' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 15. `{TOOL_API_SURFACE}`

**Found in:** `tool-vexflow.agent.md`
**Context:** The specialist agent for 'VexFlow' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 16. `{TOOL_COMMON_PATTERNS}`

**Found in:** `tool-vexflow.agent.md`
**Context:** The specialist agent for 'VexFlow' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 17. `{TOOL_DOCS_URL}`

**Found in:** `tool-vite.agent.md`
**Context:** The specialist agent for 'Vite' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 18. `{TOOL_API_SURFACE}`

**Found in:** `tool-vite.agent.md`
**Context:** The specialist agent for 'Vite' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 19. `{TOOL_COMMON_PATTERNS}`

**Found in:** `tool-vite.agent.md`
**Context:** The specialist agent for 'Vite' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 20. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ajv-reference.md`
**Context:** The reference file for 'AJV' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 21. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ajv-reference.md`
**Context:** The reference file for 'AJV' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 22. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ajv-reference.md`
**Context:** The reference file for 'AJV' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 23. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-eslint-plugin-security-reference.md`
**Context:** The reference file for 'eslint-plugin-security' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 24. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-eslint-plugin-security-reference.md`
**Context:** The reference file for 'eslint-plugin-security' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 25. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-eslint-plugin-security-reference.md`
**Context:** The reference file for 'eslint-plugin-security' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 26. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-eslintjs-reference.md`
**Context:** The reference file for 'eslintjs' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 27. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-eslintjs-reference.md`
**Context:** The reference file for 'eslintjs' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 28. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-eslintjs-reference.md`
**Context:** The reference file for 'eslintjs' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 29. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jest-reference.md`
**Context:** The reference file for 'jest' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 30. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jest-reference.md`
**Context:** The reference file for 'jest' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 31. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jest-reference.md`
**Context:** The reference file for 'jest' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 32. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jest-environment-jsdom-reference.md`
**Context:** The reference file for 'jest-environment-jsdom' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 33. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jest-environment-jsdom-reference.md`
**Context:** The reference file for 'jest-environment-jsdom' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 34. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jest-environment-jsdom-reference.md`
**Context:** The reference file for 'jest-environment-jsdom' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 35. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jestglobals-reference.md`
**Context:** The reference file for 'jestglobals' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 36. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jestglobals-reference.md`
**Context:** The reference file for 'jestglobals' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 37. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jestglobals-reference.md`
**Context:** The reference file for 'jestglobals' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 38. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tone-reference.md`
**Context:** The reference file for 'tone' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 39. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tone-reference.md`
**Context:** The reference file for 'tone' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 40. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tone-reference.md`
**Context:** The reference file for 'tone' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 41. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-web-audio-api-reference.md`
**Context:** The reference file for 'web-audio-api' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 42. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-web-audio-api-reference.md`
**Context:** The reference file for 'web-audio-api' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 43. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-web-audio-api-reference.md`
**Context:** The reference file for 'web-audio-api' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 44. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-web-midi-api-reference.md`
**Context:** The reference file for 'web-midi-api' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 45. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-web-midi-api-reference.md`
**Context:** The reference file for 'web-midi-api' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 46. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-web-midi-api-reference.md`
**Context:** The reference file for 'web-midi-api' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 47. `{COMPONENT_SPEC}`

**Found in:** `audio-engine-expert.agent.md`
**Context:** Component 'Audio Engine' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 48. `{COMPONENT_SECTIONS}`

**Found in:** `audio-engine-expert.agent.md`
**Context:** Component 'Audio Engine' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 49. `{COMPONENT_SOURCES}`

**Found in:** `audio-engine-expert.agent.md`
**Context:** Component 'Audio Engine' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 50. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `audio-engine-expert.agent.md`
**Context:** Component 'Audio Engine' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 51. `{COMPONENT_SPEC}`

**Found in:** `data-model-expert.agent.md`
**Context:** Component 'Data Model and Score I/O' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 52. `{COMPONENT_SECTIONS}`

**Found in:** `data-model-expert.agent.md`
**Context:** Component 'Data Model and Score I/O' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 53. `{COMPONENT_SOURCES}`

**Found in:** `data-model-expert.agent.md`
**Context:** Component 'Data Model and Score I/O' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 54. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `data-model-expert.agent.md`
**Context:** Component 'Data Model and Score I/O' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 55. `{COMPONENT_SPEC}`

**Found in:** `drag-reorder-expert.agent.md`
**Context:** Component 'Staff System Drag-and-Drop Reorder' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 56. `{COMPONENT_SECTIONS}`

**Found in:** `drag-reorder-expert.agent.md`
**Context:** Component 'Staff System Drag-and-Drop Reorder' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 57. `{COMPONENT_SOURCES}`

**Found in:** `drag-reorder-expert.agent.md`
**Context:** Component 'Staff System Drag-and-Drop Reorder' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 58. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `drag-reorder-expert.agent.md`
**Context:** Component 'Staff System Drag-and-Drop Reorder' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 59. `{COMPONENT_SPEC}`

**Found in:** `interactions-expert.agent.md`
**Context:** Component 'Interactions and Editing' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 60. `{COMPONENT_SECTIONS}`

**Found in:** `interactions-expert.agent.md`
**Context:** Component 'Interactions and Editing' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 61. `{COMPONENT_SOURCES}`

**Found in:** `interactions-expert.agent.md`
**Context:** Component 'Interactions and Editing' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 62. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `interactions-expert.agent.md`
**Context:** Component 'Interactions and Editing' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 63. `{COMPONENT_SPEC}`

**Found in:** `midi-engine-expert.agent.md`
**Context:** Component 'MIDI Engine' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 64. `{COMPONENT_SECTIONS}`

**Found in:** `midi-engine-expert.agent.md`
**Context:** Component 'MIDI Engine' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 65. `{COMPONENT_SOURCES}`

**Found in:** `midi-engine-expert.agent.md`
**Context:** Component 'MIDI Engine' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 66. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `midi-engine-expert.agent.md`
**Context:** Component 'MIDI Engine' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 67. `{COMPONENT_SPEC}`

**Found in:** `music-notation-expert.agent.md`
**Context:** Component 'Music Notation' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 68. `{COMPONENT_SECTIONS}`

**Found in:** `music-notation-expert.agent.md`
**Context:** Component 'Music Notation' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 69. `{COMPONENT_SOURCES}`

**Found in:** `music-notation-expert.agent.md`
**Context:** Component 'Music Notation' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 70. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `music-notation-expert.agent.md`
**Context:** Component 'Music Notation' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 71. `{COMPONENT_SPEC}`

**Found in:** `node-build-security-expert.agent.md`
**Context:** Component 'Node.js Build Pipeline and Security Layer' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 72. `{COMPONENT_SECTIONS}`

**Found in:** `node-build-security-expert.agent.md`
**Context:** Component 'Node.js Build Pipeline and Security Layer' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 73. `{COMPONENT_SOURCES}`

**Found in:** `node-build-security-expert.agent.md`
**Context:** Component 'Node.js Build Pipeline and Security Layer' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 74. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `node-build-security-expert.agent.md`
**Context:** Component 'Node.js Build Pipeline and Security Layer' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 75. `{COMPONENT_SPEC}`

**Found in:** `notation-renderer-expert.agent.md`
**Context:** Component 'Notation Renderer' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 76. `{COMPONENT_SECTIONS}`

**Found in:** `notation-renderer-expert.agent.md`
**Context:** Component 'Notation Renderer' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 77. `{COMPONENT_SOURCES}`

**Found in:** `notation-renderer-expert.agent.md`
**Context:** Component 'Notation Renderer' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 78. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `notation-renderer-expert.agent.md`
**Context:** Component 'Notation Renderer' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 79. `{COMPONENT_SPEC}`

**Found in:** `playback-engine-expert.agent.md`
**Context:** Component 'Playback Engine' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 80. `{COMPONENT_SECTIONS}`

**Found in:** `playback-engine-expert.agent.md`
**Context:** Component 'Playback Engine' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 81. `{COMPONENT_SOURCES}`

**Found in:** `playback-engine-expert.agent.md`
**Context:** Component 'Playback Engine' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 82. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `playback-engine-expert.agent.md`
**Context:** Component 'Playback Engine' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 83. `{COMPONENT_SPEC}`

**Found in:** `test-suite-expert.agent.md`
**Context:** Component 'Test Suite' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 84. `{COMPONENT_SECTIONS}`

**Found in:** `test-suite-expert.agent.md`
**Context:** Component 'Test Suite' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 85. `{COMPONENT_SOURCES}`

**Found in:** `test-suite-expert.agent.md`
**Context:** Component 'Test Suite' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 86. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `test-suite-expert.agent.md`
**Context:** Component 'Test Suite' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 87. `{COMPONENT_SPEC}`

**Found in:** `ui-controls-expert.agent.md`
**Context:** Component 'UI Controls and Event Listeners' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 88. `{COMPONENT_SECTIONS}`

**Found in:** `ui-controls-expert.agent.md`
**Context:** Component 'UI Controls and Event Listeners' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 89. `{COMPONENT_SOURCES}`

**Found in:** `ui-controls-expert.agent.md`
**Context:** Component 'UI Controls and Event Listeners' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 90. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ui-controls-expert.agent.md`
**Context:** Component 'UI Controls and Event Listeners' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---


Once all items above are resolved, invoke `@conflict-auditor` to verify consistency.
<!-- AGENTTEAMS:END content -->
